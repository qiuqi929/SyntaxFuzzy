package pool.implement;

public class TypePool {

    /**
     * The type is only String. We dont need to add a class type.
     * If we have new defined type by generate, we can add to this pool.
     */

    /**
     * We can use it to random one type.
     */

}
